### Install Node
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.1/install.sh | bash

## Add to zshrc:
export NVM_DIR="$HOME/.nvm"
# This loads nvm
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
# This loads nvm bash_completion
[ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"

nvm install
# in frontend root, run:
nvm use
node -v

### Install NPM
nvm install-latest-npm
npm -v

### Update packages
npx npm-check-updates


nvm install 21.6.2
node -v
nvm use 21.6.2
nvm alias default 21.6.2
nvm alias 21 21.6.2

# update pnpm:
npm install -g pnpm
pnpm store prune
pnpm cache clean
pnpm -v

## Generate Types
npx openapi-typescript http://0.0.0.0:8123/api/v1/openapi.json --output api-types.d.ts


pnpm dlx shadcn-ui@latest add tabs


DEPLOYMENT=container NEXT_TELEMETRY_DISABLED=1 NODE_ENV=production pnpm build

### Test build locally:
cp .env.placeholder .env.production
DEPLOYMENT=local pnpm build
source .env
export $(grep -v '^#' .env. | xargs)
export $(grep -v '^#' .env.development | xargs)
# comment out .env and .env.development
DEBUG=1 ./start.sh echo "done"
pnpm start


pnpm list @opentelemetry/api --depth=999
pnpm list react@19.0.0-rc-45804af1-20241021 --depth=999

## Unused files:
pnpm create @knip/config --yes
pnpm knip

### Run on device:
- see docs
